<배열 메소드 관련>
slice : (a,b)라는 매개변수가 들어가며 시작점, 끝점이전까지 인덱스를 가져다가
ex)
    let fruit = [apple,pear,banana]
    fruit.slice(1,2)
    
    결과 : fruit = [pear] 라는 인덱스만을 가지는 새로운 배열이 된다.
    
splice : (a,b)라는 매개변수가 들어가며 시작점으로 부터 몇 개의 아이템을 제거하고자 할 때 쓰이는 메소드 
- <★기존에 있는 배열에 영향을 준다★>

